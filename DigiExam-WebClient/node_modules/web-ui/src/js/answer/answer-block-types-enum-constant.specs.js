describe("AnswerBlockTypesEnum", function() {
	var AnswerBlockTypesEnum;

	beforeEach(function() {
		module("dx.webui.answer");
		inject(function($injector) {
			AnswerBlockTypesEnum = $injector.get("AnswerBlockTypesEnum");
		});
	});

	it("is frozen", function() {
		expect(Object.isFrozen(AnswerBlockTypesEnum)).toBeTruthy();
	});

	it("has the correct types", function() {
		expect(AnswerBlockTypesEnum.HTML).toEqual("html");
	});
});
